{
  "name": "redux-thunk-state-adapter",
  "version": "1.1.1",
  "description": "A utility to provide a facade of getState to an externally owned thunk.",
  "main": "dist/bundle.umd.js",
  "jsnext:main": "dist/bundle.es.js",
  "author": "Bradley J. Spaulding",
  "license": "MIT",
  "scripts": {
    "build": "rollup -c --environment format:umd && rollup -c --environment format:es && rollup -c --environment format:cjs",
    "test": "jest"
  },
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "jest": "^20.0.1",
    "jest-babel": "^1.0.1",
    "prettier": "^1.6.1",
    "rollup": "^0.41.6",
    "rollup-plugin-babel": "^2.7.1"
  }
}
